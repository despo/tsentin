= link_to "τσεντ.ίν", root_path, :class => 'brand'
.navbar-text.span4 τοπικές προσφορές γιατί το τσεντίν εφκέρωσεν
%ul.nav.pull-right
  %li.dropdown
    %a{ href: "#", class: "dropdown-toggle", :"data-toggle" => "dropdown"}
      -if user_signed_in?
        =current_user.email
      %b.icon-cog.icon-white
      %b.caret
    %ul.dropdown-menu
      -if user_signed_in?
        %li= link_to "Οι προσφορές μου",  my_account_path
        %li.divider
        %li= link_to "Διαχείρηση λογαριασμού",  edit_user_registration_path
        %li= link_to('Αποσύνδεση', destroy_user_session_path, :method=>'delete')
      - else
        %li= link_to('Συνδεθείτε', new_user_session_path)
        %li.divider
        %li= link_to(t('forgot_password'), new_user_password_path)
        %li= link_to('Δημιουργία λογαριασμού', new_user_registration_path)
